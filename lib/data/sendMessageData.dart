class sendMessageData{
  var messagename;
  var messageimg;
  var message;
  var timestamp;
  var userid;
  var likecount;
  var commentcount;

  sendMessageData(this.messagename, this.messageimg, this.message, this.timestamp, this.userid, this.likecount, this.commentcount);
}